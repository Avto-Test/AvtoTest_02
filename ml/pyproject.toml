[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autotest-ml"
version = "0.1.0"
description = "AutoTest ML package"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["ml"]
package-dir = {"ml" = "."}